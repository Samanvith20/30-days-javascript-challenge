const array = [1, 2, 3, 4, 5];
array.forEach((element) => {
  console.log(element);
});



// Use a for loop when you need more control over the iteration process or are working with non-array iterables.
// Use forEach when you are working specifically with arrays and want a cleaner, more readable iteration syntax without needing to break out of the loop.